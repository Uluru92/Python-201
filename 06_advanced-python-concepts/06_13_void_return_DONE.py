# Write a lambda function that does not take in an arguments 
# but returns a value. Print the return value.

#solution

y = "playing around with this lambda thing"
x = "Me"
writing_something = lambda: x+" "+y
print(writing_something())
